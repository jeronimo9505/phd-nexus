
import Login from '@/components/modules/Login/Login';

export const metadata = {
    title: 'Login - PhD Nexus',
};

export default function LoginPage() {
    return <Login />; // Ensure Login module has 'use client'
}
